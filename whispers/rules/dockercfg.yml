- id: dockercfg
  description: Hardcoded .dockercfg auth
  message: Dockercfg auth
  severity: CRITICAL
  key:
    regex: ^dockercfg auth$
    ignorecase: False
  value:
    isBase64: True
    isAscii: True
    minlen: 1
